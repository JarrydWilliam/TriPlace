name: Seed Database
on:
  push:
    branches:
      - main

jobs:
  seed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Run Seed
        env:
          DATABASE_URL: postgresql://neondb_owner:npg_B1ZOy8rXeHvR@ep-dark-frog-an1nq49i-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
        run: npx tsx scripts/seed-premium.ts
