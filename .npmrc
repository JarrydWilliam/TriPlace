# Force npm to install optional dependencies for all platforms
# Required because package-lock.json is generated on Windows but Vercel runs on Linux
# Without this, Vercel gets "Cannot find module @rollup/rollup-linux-x64-gnu"
optional=true
